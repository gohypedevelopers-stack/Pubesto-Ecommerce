export const metadata = {
  title: "Cancel Order | Pubesto",
  description: "Submit a cancellation request for a Pubesto order before dispatch.",
};

export default function CancelOrderLayout({ children }) {
  return children;
}

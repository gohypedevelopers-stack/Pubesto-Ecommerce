export const metadata = {
  title: "Request a Return | Pubesto",
  description:
    "Submit a return request for your Pubesto order. We offer 7-day worry-free returns on all orders.",
};

export default function ReturnsLayout({ children }) {
  return children;
}
